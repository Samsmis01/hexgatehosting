{
  "name": "momozentech",
  "version": "2.0.0",
  "description": "MOMO-ZEN WhatsApp Pairing System - Version Render Optimisée",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:bot": "node index.js",
    "start:combined": "node start-combined.js",
    "dev": "nodemon server.js",
    "dev:bot": "nodemon index.js",
    "dev:combined": "nodemon start-combined.js",
    "render": "node render-start.js",
    "test": "node test-render.js",
    "health": "curl -s https://${RENDER_SERVICE_NAME:-localhost}:${PORT:-3000}/health || echo 'Test local: curl http://localhost:3000/health'",
    "build": "npm install && mkdir -p public commands viewOnce deleted_messages deleted_images .VV auth_info_baileys",
    "postinstall": "echo '✅ Dépendances installées. Pour Render: npm run render'",
    "logs": "echo '📋 Connectez-vous à Render Dashboard pour voir les logs'",
    "clean": "rm -rf node_modules package-lock.json",
    "reset": "rm -rf auth_info_baileys node_modules && npm install",
    "backup": "cp config.json config.backup.json && echo '💾 Backup créé'"
  },
  "keywords": ["whatsapp", "bot", "pairing", "api", "render", "hexgate"],
  "author": "HEX-TECH",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.5.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "express-rate-limit": "^7.1.5",
    "pino": "^8.19.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
